import { useQuery } from "react-query";
import { useParams } from "react-router-dom";

export const User = () => {
  // const { id } = useParams();
  // useQuery(['user',]);

  return <div>User</div>;
};
